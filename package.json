{
  "name": "graphif-mcp-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "graphif-mcp",
    "graphif-live-plugin"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run build && node graphif-mcp/dist/server.js",
    "test": "npm test --workspace graphif-mcp"
  }
}
