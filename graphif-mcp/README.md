# Graphif MCP Package

This package contains the local MCP server implementation for reading, creating, and modifying Graphif `.prg` files.

Use the workspace root README for setup, MCP configuration, and deployment instructions.
