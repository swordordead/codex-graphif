# Graphif MCP Package

This package contains the local MCP server implementation for:

- reading, creating, and modifying Graphif `.prg` files;
- hosting the localhost live bridge at `127.0.0.1:17321`;
- exposing live MCP tools that forward commands to the Graphif extension.

Use the workspace root README for setup, MCP configuration, live plugin installation, and deployment instructions.
